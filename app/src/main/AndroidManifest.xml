<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greedygame.testapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:name=".ui.BaseApplication"
        android:theme="@style/Theme.TestApp">
        <activity
            android:name=".ui.news_dashboard.NewsDashboardActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.news_description.NewsDescriptionActivity"
            android:exported="true"
            android:theme="@style/Theme.TestApp.NoActionBar"
            />
        <activity android:name=".ui.save_to_read.SaveToReadActivity"
            android:exported="true"
            android:theme="@style/Theme.TestApp.NoActionBar"
            />
    </application>

</manifest>